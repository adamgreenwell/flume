# `engines` is advisory unless this is set: without it npm installs into a
# known-broken combination and the failure surfaces later, as a vitest worker
# crash rather than a version error.
engine-strict=true
